import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export default function TeamsPage() {
    return (
        <Card>
            <CardHeader>
                <CardTitle>Teams</CardTitle>
                <CardDescription>Team and user management will be available here.</CardDescription>
            </CardHeader>
            <CardContent>
                <p>Coming soon...</p>
            </CardContent>
        </Card>
    )
}
